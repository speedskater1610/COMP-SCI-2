# DeMorgans's law

DeMorgan's law shows how negation interacts with "and" and "or" operations with the following:

- `!(A && B)` <-> `!A || !B`
- `!(A || B)` <-> `!A && !B`

To prove this you can write a truth table.
