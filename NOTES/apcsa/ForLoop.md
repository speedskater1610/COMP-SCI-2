# for loops

A for loops is a loop that executes a "known amount" of times.

Syntax:
```java
for (int i = 0; i < 10; ++i) {
      -----      ----    ---
      Control | condition | cv
        var   |           | update

    // Code to run...
}
```
