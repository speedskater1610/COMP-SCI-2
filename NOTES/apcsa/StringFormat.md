# String Formatting

## `String.format();`

The `String.format()` method is a **static method** of the `String` class. The method returns a constructed `String`. 

*NOTE: it uses the same formatters are `System.out.printf()`*
