# Largest value
Write a method called maxValue that will return the largest value of four integers. 
```java
public int maxValue(int num1, int num2, int num3, int num4) {
    return Math.max(Math.max(num1, num2), Math.max(num3, num4));
}
```
